# Migration from iperf3-test-suite

- throughput entrypoint moved to `apps/throughput/iPerf3Test.ps1`
- shared orchestration lives at `Invoke-NetworkDiagnostics.ps1`
- repo identity is now broader: throughput is one module inside the suite
