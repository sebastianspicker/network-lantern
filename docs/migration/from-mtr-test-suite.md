# Migration from mtr-test-suite

- bash entrypoint moved to `apps/path/mtr-test-suite.sh`
- PowerShell entrypoint moved to `apps/path/NetPathSuite.ps1`
- `config/hosts.conf` remains at repo root
- path diagnostics now sit beside throughput and optional Windows tuning workflows
